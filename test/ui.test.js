const test=require('node:test');const assert=require('node:assert/strict');const fs=require('fs');const path=require('path');const {JSDOM}=require('jsdom');
test('UI renders the complete five-stage workflow and restores session data',async()=>{const html=fs.readFileSync(path.join(__dirname,'../public/index.html'),'utf8').replace('<script src="/app.js"></script>','');const dom=new JSDOM(html,{url:'http://localhost/',runScripts:'outside-only'});dom.window.sessionStorage.setItem('ict-review-state',JSON.stringify({query:'저장된 질의',reviewer:{name:'검토자',department:'평가팀',finalComment:''}}));dom.window.eval(fs.readFileSync(path.join(__dirname,'../public/app.js'),'utf8'));dom.window.document.dispatchEvent(new dom.window.Event('DOMContentLoaded'));assert.equal(dom.window.document.querySelectorAll('.steps button').length,5);assert.equal(dom.window.document.querySelectorAll('#eligibilityBody tr').length,5);assert.equal(dom.window.document.querySelectorAll('[data-dim]').length,8);assert.equal(dom.window.document.querySelector('#query').value,'저장된 질의');assert.equal(dom.window.document.querySelector('#reviewerName').value,'검토자');assert.equal(dom.window.document.querySelectorAll('.downloads button').length,3);});
